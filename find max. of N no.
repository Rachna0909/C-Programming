#include<stdio.h>;
int main(){
    int i, n, max=0, num;
    printf("Enter the value of N");
    scanf("%d", &n);
    for(i=1; i<=n; i++){
       printf("Enter number ");
       scanf("%d", &num);
       if(num>max)
        max=num;
    }
           return 0;
}
