#include<stdio.h>
int main(){
     int num;
     printf("Enter number ");
     scanf("%d", &num);
     if(num%2 == 0)
     {
        int cube = num*num*num;
        printf("Cube of %d is %d", num, cube);
     }
     return 0;
    }
