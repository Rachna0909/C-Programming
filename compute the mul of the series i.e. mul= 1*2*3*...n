#include<stdio.h>;
int main(){
    int i,n,mul=1;
    printf("Enter the value of N");
    scanf("%d", &n);
    for(i=1; i<=n; i++){
        mul *= i;
    }
        printf("Sum = %d", mul);
    return 0;
}
