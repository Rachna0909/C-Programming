#include<stdio.h>
int main(){
    int num;
    printf("Enter number :");
    scanf("%d", &num);
    if(num%5==0)
    if(num%7==0)
      printf("Number is divisible ");
    else
      printf("Number is not divisible");
      return 0;
}
